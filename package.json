{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "dev-https": "nuxt dev --https --ssl-cert localhost.pem --ssl-key localhost-key.pem",
    "start-https": "nuxt start --https --ssl-cert localhost.pem --ssl-key localhost-key.pem"
  },
  "devDependencies": {
    "nuxt": "3.0.0-rc.13"
  },
  "dependencies": {
    "@nuxtjs/color-mode": "^3.1.8",
    "@nuxtjs/robots": "^2.5.0",
    "@nuxtjs/tailwindcss": "^6.1.3",
    "daisyui": "^2.38.1"
  }
}